# PRD — 에인연 모바일 퍼스트 재구축

브랜치: `feature/mobile-rebuild` · main 배포 금지 · 2026-07-04

> 현재 라우트(2026-07-10): P1 이후 모닝브리핑은 `/` 홈으로 통합됐고 `/briefing/`은
> 홈 리다이렉트다. 아래 정보 구조와 Lighthouse 수치는 모바일 재구축 당시 기록이다.

## 1. 배경 / 사용자

- 사용자: HVAC·인테리어 **현장 종사자**. 70%+ 모바일 접속 가정.
- 사용 환경: 야외·이동 중·장갑 낀 손. **한 손 조작, 엄지 도달 범위** 중심.
- 핵심 여정: 출근길 3분 안에 "오늘 시세 → 이번 주 입주 → 새 공고"를 확인하고 닫는다.

## 2. 모바일 UX 원칙

1. **엄지 우선**: 핵심 내비게이션은 화면 하단 고정 탭바. 상단 nav는 보조.
2. **터치 타겟 ≥ 44×44px**: 탭바·버튼·칩·카드 링크 전부. 인접 타겟 간격 ≥ 8px.
3. **본문 ≥ 16px**: 야외 시인성. 보조 텍스트도 13px 미만 금지.
4. **모션 경량화**: 마우스 파랙스·3D 틸트는 모바일(coarse pointer) 비활성 (기존 가드 유지).
   스크롤 리빌은 유지하되 blur 강도 축소(8px→4px) — 저사양 기기 페인트 비용 절감.
5. **오프라인 내성**: 현장 음영지역 대비 — 마지막 브리핑·시세 1회분은 비행기모드에서도 열람.
6. **데스크톱 무회귀**: ≥900px 화면은 기존 motionsites 스타일 그대로. 탭바는 모바일 전용.

## 3. 정보 구조 — 하단 탭바 (5탭)

| 탭 | 경로 | 상태 |
|---|---|---|
| 홈 | `/` | 기존 |
| 입주레이더 | `/calendar/` | 기존 |
| 모닝브리핑 | `/briefing/` | 기존 |
| 시세 | `/prices/` | **신규** (v_ticker_metals 기반 요약 페이지) |
| 내정보 | `/me/` | **신규** (세션 표시 + 로그아웃. 로그인 배선은 기존 ainAuth 재사용, OAuth 설정 무변경) |

## 4. 완료 기준 (Definition of Done)

- [x] 375px 뷰포트에서 전 페이지 가로 스크롤 0 (scrollX 이동 불가로 검증 — 6페이지)
- [x] 탭바·주요 버튼 터치 타겟 44×44px 이상 (측정 스크립트 — 탭바 최소 56px, 위반 0건)
- [x] 본문 텍스트 16px 이상 (브리핑 line-item·뉴스 요약·캘린더 단지명 16px)
- [x] 모바일에서 파랙스·틸트 미동작(coarse pointer 가드), 리빌 동작·경량화(blur 8→4px)
- [x] Pretendard 서브셋 woff2 ≤ 900KB → **320KB** (KS X 1001 + wght 400–900 인스턴스), swap 유지
- [x] 전 페이지 canonical·og:url·og:title·og:description·og:image(절대 URL) 완비
- [x] manifest.json + 아이콘(192/512/maskable) + 서비스워커 (설치 프롬프트 포함)
- [x] 비행기모드에서 브리핑·시세 페이지 마지막 캐시본 열람 (Playwright offline 검증)
- [~] Lighthouse 모바일 (gzip 서버, 콜드 캐시): 홈 **91**/me 95/prices 88/calendar 82/briefing 75/news 71 —
      A11y·BP·SEO는 **전 페이지 100**. 데이터 페이지 감점은 fetch-bound LCP (HANDOFF 참고)
- [x] 데스크톱(1440px) 기존 렌더 회귀 없음 (탭바 숨김·히어로 52px·티커 정상)

## 5. 비범위 (이번 작업에서 안 함)

- main 머지·배포 (아침 수동)
- Kakao OAuth 설정·redirect URI 변경 (기존 배선 그대로 사용만)
- Supabase SQL·스키마 변경
- 구인구직·템플릿 메이커 등 신규 기능

## 6. 리스크

- 서비스워커 캐시 무효화 실수 → 버전드 캐시 키 + 네트워크 우선 전략으로 완화
- 폰트 서브셋 글자 누락(희귀 한자·특수문자) → 한글 전체 음절(U+AC00–D7A3)+라틴+약물 포함으로 완화
- Lighthouse 점수는 로컬 python 서버 기준 — 실배포(GitHub Pages CDN)에서 재측정 필요
